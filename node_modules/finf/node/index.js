'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});
exports['default'] = finf;

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

var _fs = require('fs');

var _fs2 = _interopRequireDefault(_fs);

var _file_pattern = require('./file_pattern');

var _file_pattern2 = _interopRequireDefault(_file_pattern);

function finf(path, pattern) {

  try {
    var err = _fs2['default'].accessSync(path, _fs2['default'].R_OK);
    if (err != undefined) {
      return [err, null];
    }
  } catch (e) {
    return [e, null];
  }

  var list = _fs2['default'].readdirSync(path);
  var files = [];

  if (pattern != undefined) {
    pattern = (0, _file_pattern2['default'])(pattern);
    for (var i = 0; i < list.length; i++) {
      var file = list[i];
      if (pattern.test(file)) files.push(file);
    }
  } else {
    files = list;
  }
  return [null, files];
}

module.exports = exports['default'];

